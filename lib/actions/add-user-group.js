var utils = require('../utils');

var Action = utils.getActionClass();

module.exports = function(name, callback) {
	var userGroup =  {
		name: name,
		description: 'Generated by LWS'
	};

	var action = new Action('/usergroup/add-user-group', userGroup);

	action.doAction(callback);
}